{% extends 'base.html.twig' %}

{% block title %}Page de l'auteur : {{ auteur.firstname }}{% endblock %}

{% block content %}
    <h1>Vous êtes sur le profil de "{{ auteur.firstname | capitalize }}"</h1>
    <div class="authorProfile">
    <i class="fas fa-user-ninja fa-8x"></i>
        <h4>{{ auteur.lastname }}</h4>
        <h4>{{ auteur.firstname }}</h4>
    </div>
    <div class="buttonsRow">
        <a class="btn btn-update" href="{{ path('author_edit', {'id': auteur.id}) }}">Modifier</a>
        <a class="btn btn-delete" href="{{ path('author_delete', {'id': auteur.id}) }}">Supprimer</a>
    </div>
{% endblock %}